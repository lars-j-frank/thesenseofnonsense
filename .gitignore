# Hugo output
/public/
/resources/_gen/
/hugo_stats.json

# OS
.DS_Store
Thumbs.db

# Editor / agent local state (may contain absolute user paths)
.vscode/
.idea/
.cursor/
*.swp
*.swo

# Environment / secrets
.env
.env.*
*.pem
*.key
secrets/
**/credentials.json

# Hugo local lock (not needed in remote)
.hugo_build.lock

# Local research scrapes / PDFs (too large for public repo; keep off Decap)
/research/
